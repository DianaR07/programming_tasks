using HorizonSideRobots

function towall!(robot::Robot, side) # робот идет до стенки рекурсивным способом в заданном направлении
    if !isborder(robot, side)
        move!(robot, side)
        towall!(robot, side)
    end
end

#робот начинает движение на клетке, которая не является стеной. Он будет
продолжать двигаться в указанном направлении до тех пор, пока не наткнется на стену. Каждый
раз, когда он перемещается, функция towall! вызывает саму себя для проверки и движения
дальше.
